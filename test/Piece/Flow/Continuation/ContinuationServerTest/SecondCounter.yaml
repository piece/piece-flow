# $Id$
name: SecondCounter
firstState: DisplayForm
lastState: { name: Finish, view: Finish }
viewState:
  - name: DisplayForm
    view: SecondCounter
    entry:
      method: setup
    transition:
      - event: increase
        nextState: increased
        action:
          method: increase
actionState:
  - name: increased
    transition:
      - event: succeed
        nextState: DisplayForm
