# $Id$
firstState: starting
initial:
  method: initialize
final:
  method: finalize
viewState:
  - name: starting
    view: start
    transition:
      - event: go
        nextState: ending
actionState:
  - name: ending
    transition:
      - event: untriggered
        nextState: starting
