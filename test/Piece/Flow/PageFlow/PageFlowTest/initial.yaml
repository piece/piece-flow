# $Id$
firstState: starting
initial:
  method: initialize
final:
  method: finalize
lastState: { name: ending, view: end }
viewState:
  - name: starting
    view: start
    transition:
      - event: go
        nextState: ending
